# Astro 项目目录结构说明（Global-link-Company-Profile）

适用版本：Astro 5 + React（`@astrojs/react`）+ Tailwind CSS（通过 Vite 插件）

## 先理解 3 个 Astro 关键点

1. **基于文件的路由（File-based Routing）**：`src/pages/` 里的文件名决定访问路径。  
2. **`.astro` 组件是默认形态**：文件由两部分组成：
   - 顶部 `---` 之间：写导入、变量、数据处理（在构建/服务端执行）
   - 下面模板：写 HTML + 组件（渲染输出）
3. **交互用“岛屿”（Islands）**：需要在浏览器运行的 React 组件，要用 `client:*` 指令进行“水合/挂载”，否则只会输出静态 HTML。

## 项目结构总览

（根据当前仓库实际文件整理）

```text
/
├─ public/
│  └─ favicon.svg
├─ src/
│  ├─ components/
│  │  ├─ home/            # 首页各分区（Hero、CTA、Testimonials…）
│  │  ├─ react/           # 需要交互的 React 组件（client:*）
│  │  ├─ ui/              # 可复用 UI 小组件（Button、Card、Section…）
│  │  ├─ Footer.astro
│  │  └─ Header.astro
│  ├─ config/
│  │  └─ site.ts          # 站点标题、导航、社媒等配置
│  ├─ layouts/
│  │  └─ BaseLayout.astro # 全局布局：引入样式、Header/Footer、SEO meta、slot
│  ├─ pages/              # 路由页面（访问路径由文件名决定）
│  │  ├─ index.astro
│  │  ├─ capabilities.astro
│  │  ├─ use-cases.astro
│  │  ├─ facilities.astro
│  │  ├─ documentation.astro
│  │  └─ rfq.astro
│  ├─ styles/
│  │  └─ global.css       # Tailwind 引入 + 少量全局样式/动画
│  └─ utils/
│     └─ constants.ts     # 业务常量（行业、能力点等）
├─ astro.config.mjs       # Astro 集成与构建配置（React、Vite 插件）
├─ package.json           # 依赖与脚本（dev/build/preview）
└─ tsconfig.json          # TS 严格配置 + 路径别名（@components 等）
```

## 根目录文件（`/`）

### `package.json`

- **用途**：管理依赖与脚本。
- **常用脚本**：
  - `npm run dev`：启动开发服务器（默认 `http://localhost:4321`）
  - `npm run build`：构建到 `dist/`
  - `npm run preview`：本地预览构建产物

### `astro.config.mjs`

- **用途**：Astro 的主配置。
- **你项目里做了什么**：
  - 启用 React：`integrations: [react()]`
  - 给 Vite 加 Tailwind 插件：`vite.plugins: [tailwindcss()]`

### `tsconfig.json`

- **用途**：TypeScript 配置 + 路径别名（写导入更顺手）。
- **你项目里定义的别名**：
  - `@/*` → `src/*`
  - `@components/*` → `src/components/*`
  - `@layouts/*` → `src/layouts/*`
  - `@config/*` → `src/config/*`
  - `@utils/*` → `src/utils/*`

## `public/`（静态资源目录）

- **用途**：放“原样对外可访问”的静态文件（不会经过打包处理）。
- **访问规则**：`public/favicon.svg` 可以用 `/favicon.svg` 访问。
- **适合放**：站点图标、robots.txt、静态图片、下载文件等。

## `src/pages/`（页面与路由）

- **用途**：每个 `.astro` 文件默认就是一个路由页面。
- **路由规则**：
  - `src/pages/index.astro` → `/`
  - `src/pages/capabilities.astro` → `/capabilities`
  - `src/pages/use-cases.astro` → `/use-cases`
  - `src/pages/facilities.astro` → `/facilities`
  - `src/pages/documentation.astro` → `/documentation`
  - `src/pages/rfq.astro` → `/rfq`
- **页面组织方式（你项目的做法）**：页面通常只做“编排”，把具体内容拆到 `src/components/**`。

## `src/layouts/`（布局）

### `src/layouts/BaseLayout.astro`

- **用途**：所有页面共用的外壳：全局样式、头部/底部、SEO meta、`<slot />` 内容占位。
- **你项目里做了什么**：
  - 引入全局样式：`import '../styles/global.css'`
  - 引入通用头尾：`Header` / `Footer`
  - 从 `src/config/site.ts` 读取默认标题与描述：`SITE.title` / `SITE.description`
  - 把页面主体渲染在 `<slot />` 中

## `src/components/`（组件）

### `src/components/Header.astro` 与 `src/components/Footer.astro`

- **Header**：读取 `NAVIGATION`（在 `src/config/site.ts`）生成菜单；移动端菜单用 React 组件 `MobileMenu`，并通过 `client:idle` 在浏览器空闲时挂载。
- **Footer**：通常放版权、社媒链接、站点导航等（按你项目的具体内容为准）。

### `src/components/home/`

- **用途**：首页分区组件（如 `PremiumHero.astro`、`Testimonials.astro` 等）。
- **用法**：`src/pages/index.astro` 里依次引入并组合这些分区。

### `src/components/ui/`

- **用途**：抽出来的通用 UI 组件（Button、Card、Section、TestimonialCard）。
- **建议**：当某个 UI 在多页复用时，优先放这里，避免页面组件越来越臃肿。

### `src/components/react/`

- **用途**：需要交互、需要在浏览器运行的 React 组件（Tabs、Accordion、表单、动效等）。
- **关键点**：在 `.astro` 里使用时，需要 `client:*` 指令决定什么时候在浏览器挂载，例如：
  - `client:load`：页面加载后立即挂载（你项目中最常见）
  - `client:idle`：浏览器空闲时再挂载（例如 `MobileMenu`）

你项目里的真实例子（便于你对照理解）：

- `src/pages/use-cases.astro` 使用 React Tabs：`<UseCaseTabs ... client:load />`
- `src/pages/capabilities.astro` 使用 React Accordion：`<CapabilityAccordion ... client:load />`
- `src/pages/rfq.astro` 使用 React 表单：`<RFQForm client:load />`

补充：像 `lucide-react` 这类 React 图标组件，也可以直接在 `.astro` 中使用；不加 `client:*` 时它会被当作“静态渲染输出”（不会在浏览器运行交互逻辑）。

## `src/config/`（配置集中管理）

### `src/config/site.ts`

- **SITE**：站点标题、描述、域名、作者等（用于 layout 的默认 SEO 信息）。
- **NAVIGATION**：顶部导航菜单的数据源（Header 会读取它生成菜单项）。
- **SOCIAL_LINKS**：社媒链接集中维护（Header/Footer 常用）。

## `src/styles/`（样式）

### `src/styles/global.css`

- **用途**：全局样式入口。
- **你项目里做了什么**：
  - `@import "tailwindcss";` 引入 Tailwind
  - 少量全局 class（例如 `.nav-active`）和动画 keyframes

## `src/utils/`（工具与常量）

### `src/utils/constants.ts`

- **用途**：集中放“跨页面会用到的固定数据/枚举/常量”。
- **你项目里已有**：`INDUSTRIES`、`CAPABILITIES` 等，供页面/组件渲染列表用。

## 最常见的 4 个改法（新手上手）

### 1）新增一个页面（路由）

在 `src/pages/` 新建文件就行，比如新增 `about.astro`：

```astro
---
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout title="About" description="About us">
  <section class="container mx-auto px-4 py-16">
    <h1 class="text-3xl font-bold">About</h1>
    <p class="mt-4 text-slate-600">这里写你的内容。</p>
  </section>
</BaseLayout>
```

然后在 `src/config/site.ts` 的 `NAVIGATION` 里加一项 `{ name: 'About', href: '/about' }`，Header 就会自动出现菜单。

### 2）新增一个可复用的 Astro 组件

例如新增 `src/components/ui/Badge.astro`，以后多处使用；页面里就 `import Badge from '@components/ui/Badge.astro'`。

### 3）新增一个需要交互的 React 组件

放到 `src/components/react/`，然后在页面/组件里用：

```astro
<MyReactWidget client:load />
```

如果不希望首屏就挂载，可以用：

- `client:idle`：更省首屏
- `client:visible`：滚动到可见区域再挂载（适合长页面）

### 4）放图片/文件应该放哪

- **不需要构建处理、希望直接用 URL 引用**：放 `public/`，然后用 `/xxx.png` 引用。
- **想统一在源码里管理（并可能配合构建工具处理）**：通常放 `src/assets/`（你这个仓库目前还没有该目录，后续你可以按需创建）。

